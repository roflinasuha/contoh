
public class EFactor extends experienceFactor {
	
	
	
	 double eFactor(){
		 double totalExperience;
		 totalExperience = expf1 + expf2 + expf3 + expf4 + expf5 + expf6 +expf7 + expf8;
    
    return totalExperience;
	 }
	 
}
